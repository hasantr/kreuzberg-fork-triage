// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3c6bb59e003c2474d25742704936d404a53e70dcd6693834c31ac8f47b9a8a29
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;
import dev.kreuzberg.ExtractionConfig;
import java.util.Optional;

/** E2e tests for category: error. */
@SuppressWarnings("checkstyle:LineLength")
class ErrorTest {
    @Test
    void testErrorEmptyBytes() throws Exception {
        // Graceful handling of empty bytes (should not error)
        var config = ExtractionConfig.builder().build();
        var result = Kreuzberg.extractBytesSync("text/empty.txt".getBytes(), "text/plain", config);
    }

    @Test
    void testErrorEmptyMime() throws Exception {
        // Error when extracting with empty MIME type
        var config = ExtractionConfig.builder().build();
        assertThrows(Exception.class, () -> Kreuzberg.extractBytesSync("text/plain.txt".getBytes(), "", config));
    }

    @Test
    void testErrorExtractBytesConflictingOcr() throws Exception {
        // extract_bytes force+disable OCR
        var config = ExtractionConfig.builder().withDisableOcr(true).withForceOcr(true).build();
        assertThrows(Exception.class, () -> Kreuzberg.extractBytesSync("text/fake_text.txt".getBytes(), "text/plain", config));
    }

    @Test
    void testErrorInvalidMimeFormat() throws Exception {
        // Error when extracting with invalid MIME type format
        var config = ExtractionConfig.builder().build();
        assertThrows(Exception.class, () -> Kreuzberg.extractBytesSync("text/plain.txt".getBytes(), "not-a-mime", config));
    }

    @Test
    void testErrorUnsupportedMime() throws Exception {
        // Error when extracting with unsupported MIME type
        var config = ExtractionConfig.builder().build();
        assertThrows(Exception.class, () -> Kreuzberg.extractBytesSync("text/plain.txt".getBytes(), "application/x-nonexistent", config));
    }

}
