// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a4fa9d89a8f719cd51011adf54dc6de60f2592234e6d9c48d0bd7ea9e366be3d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { extractFile, , type JsExtractionConfig } from 'kreuzberg';

describe('chunking', () => {
  it('chunk_batch_basic: Batch chunking of multiple texts in a single call', () => {
    ({ config: { max_characters: 100, max_overlap: 10 }, texts: ["Hello world. This is text one.", "Goodbye world. This is text two."] });
  });

  it('chunk_text_basic: Basic text chunking with small chunk size and overlap', () => {
    ({ config: { max_characters: 50, max_overlap: 10 }, text: "The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text." });
  });

  it('chunk_text_large: Text chunking with large chunk size to test handling of long content', () => {
    ({ config: { max_characters: 1000, max_overlap: 10 }, text: "The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text." });
  });

  it('chunk_text_no_overlap: Text chunking with no overlap between chunks', () => {
    ({ config: { max_characters: 50, max_overlap: 0 }, text: "The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text." });
  });

  it('chunk_text_overlap_exceeds: chunk_text overlap >= max', async () => {
    await expect(async () => {
      await ({ config: { max_characters: 50, overlap: 60 }, text: "Some text." });
    }).rejects.toThrow();
  });

  it('chunk_text_yaml: Chunk with YAML chunker', () => {
    ({ config: { chunker_type: "yaml", max_characters: 50 }, text: "key1: value1\nkey2: value2\n---\nkey3: value3" });
  });

  it('chunk_text_zero_max: chunk_text max_characters zero', async () => {
    await expect(async () => {
      await ({ config: { max_characters: 0 }, text: "Some text." });
    }).rejects.toThrow();
  });

  it('chunk_texts_batch_multiple: Batch chunk multiple texts', () => {
    ({ config: { max_characters: 100 }, texts: ["First doc text.", "Second doc text."] });
  });
});
