// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c8e6df1783f889711995d46ab6d9ec7bc698c943b0b7f97a374779a0c426c575
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from "vitest";
import { extractFile, extractFileSync, extractBytesSync } from "kreuzberg";

describe("format_specific", () => {
  it("format_csv: CSV file extraction using extract_file_sync", () => {
    const result = extractFileSync("csv/data_table.csv");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it("format_docx_standalone: Standalone DOCX extraction using extract_bytes_sync", () => {
    const result = extractBytesSync(
      null,
      "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    );
    expect(result.content.length).toBeGreaterThanOrEqual(20);
  });

  it("format_email_eml: Email EML file extraction using extract_file_sync", () => {
    const result = extractFileSync("email/html_email_multipart.eml");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it("format_pdf_text: Standalone PDF text extraction using extract_bytes_sync", () => {
    const result = extractBytesSync(null, "application/pdf");
    expect(result.content.length).toBeGreaterThanOrEqual(50);
    expect(["Mallori", "May"].some((v) => result.content.includes(v))).toBe(true);
  });

  it("format_pptx: PPTX presentation extraction using extract_bytes_sync", () => {
    extractBytesSync(
      null,
      "application/vnd.openxmlformats-officedocument.presentationml.presentation",
    );
  });

  it("format_rtf: RTF file extraction using extract_file_sync", () => {
    const result = extractFileSync("rtf/extraction_test.rtf");
    expect(result.content.length).toBeGreaterThanOrEqual(5);
  });

  it("format_xlsx: XLSX spreadsheet extraction using extract_bytes_sync", () => {
    extractBytesSync(null, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet");
  });
});
